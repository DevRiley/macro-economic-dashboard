name: Daily Macro Data Update

# 設定觸發條件
on:
  schedule:
    # 每天 UTC 時間 00:00 執行 (台灣時間早上 08:00)
    - cron: '0 0 * * *'
  workflow_dispatch: # 允許在 GitHub 網頁上手動按按鈕觸發 (方便測試)

permissions:
  contents: write  # ⚠️ 重要：賦予機器人寫入權限，不然無法把 CSV 存回去

jobs:
  update-data:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.14'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run ETL Script
        env:
          # 從 GitHub Secrets 讀取 API Key
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
        run: |
          python src/etl_script.py

      - name: Commit and Push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          # 檢查有沒有檔案變更
          git add data/*.csv
          
          # 如果有變更才 Commit 沒變更就不做動作 (避免報錯)
          git commit -m "📈 Auto-update: Daily macro data refresh" || exit 0
          
          git push