name: Daily Macro Data Update

# Trigger configuration (觸發設定)
on:
  schedule:
    # Run daily at 00:00 UTC, which is 08:00 AM Taiwan Time
    # (每天 UTC 00:00 執行，即台灣時間早上 08:00)
    - cron: '0 0 * * *'
  workflow_dispatch: # Allow manual trigger from GitHub UI for testing (允許手動按鈕觸發)

permissions:
  contents: write  # ⚠️ Important: Grant write permissions to save CSV files back to repo (賦予寫入權限)

jobs:
  update-data:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          # Use stable version 3.14 
          python-version: '3.14'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run ETL Script
        env:
          # Load API Key from GitHub Secrets (從 Secrets 讀取金鑰)
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
        run: |
          python src/etl_script.py

      - name: Commit and Push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          # Stage changed CSV files (暫存變更的 CSV 檔)
          git add data/*.csv
          
          # Commit only if there are changes to avoid errors (若有變更才提交，否則略過)
          git commit -m "📈 Auto-update: Daily macro data refresh" || exit 0
          
          git push
